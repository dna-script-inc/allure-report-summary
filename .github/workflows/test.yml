name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Display allure
        uses: dna-script-inc/allure-report-summary@master
        with:
          path: allure-results
    
